{
  "name": "frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:tradsphere": "npm --prefix shell install",
    "install:all": "npm run install:tradsphere",
    "dev:shell": "npm --prefix shell run dev",
    "dev:tradsphere": "npm run dev:shell",
    "build:shell": "npm --prefix shell run build",
    "build:tradsphere": "npm run build:shell",
    "build:all": "npm run build:shell",
    "build": "npm run build:all"
  }
}
